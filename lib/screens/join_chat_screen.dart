import 'package:flutter/material.dart';

class JoinChatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Join Chat')),
      body: Center(
        child: Text('Join Chat Screen'),
      ),
    );
  }
}
